import { NgFileSelectDirective } from './ng-file-select.directive';

describe('NgFileSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new NgFileSelectDirective();
    expect(directive).toBeTruthy();
  });
});
